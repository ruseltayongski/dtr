<?php $__env->startSection('content'); ?>
    <h3 class="page-header">Supervise Employee</h3>
    <form class="form-inline form-accept" action="<?php echo asset('location/roles'); ?>" method="GET">
        <input type="hidden" name="_token" value="<?php echo csrf_token(); ?>">
        <div class="form-group">
            <input type="text" name="search" value="<?php echo $search; ?>" class="form-control" placeholder="Quick Search" autofocus>
            <button type="submit" class="btn btn-default"><i class="fa fa-search"></i> Search</button>
        </div>
    </form><br>
    <div class="row">
        <div class="col-md-12">
            <?php if(isset($supervise_employee) and count($supervise_employee) >0): ?>
                <div class="table-responsive">
                    <table class="table table-list table-hover table-striped">
                        <thead>
                        <tr style="background-color:grey;">
                            <th>User ID</th>
                            <th>Name</th>
                            <th>Designation</th>
                            <th>Division</th>
                            <th>Section</th>
                            <th>Job Status</th>
                        </tr>
                        </thead>
                        <tbody>
                        <?php foreach($supervise_employee as $supervise): ?>
                            <tr>
                                <td><?php echo $supervise->userid; ?></td>
                                <td>
                                    <b><a href="#" data-supervise_id="<?php echo $supervise->userid; ?>" data-supervise_name="<?php echo strtoupper($supervise->name); ?>" class="supervise_view text-green"><?php echo strtoupper($supervise->name); ?></a></b>
                                </td>
                                <td><small><?php echo $supervise->designation; ?></small></td>
                                <td><small><?php echo $supervise->division; ?></small></td>
                                <td><small><?php echo $supervise->section; ?></small></td>
                                <td>JOB ORDER</td>
                            </tr>
                        <?php endforeach; ?>
                        </tbody>
                    </table>
                </div>
                <?php echo $supervise_employee->links(); ?>

            <?php else: ?>
                <div class="alert alert-danger" role="alert">Empty Records</div>
            <?php endif; ?>
        </div>
    </div>
<?php $__env->stopSection(); ?>
<?php $__env->startSection('js'); ?>
    @parent
    <script>
        $(".supervise_view").click(function(){
            $('#supervise_view').modal({
                backdrop: 'static',
                keyboard: false,
                show: true
            });
            var supervise_id = $(this).data('supervise_id');
            $("#supervise_id").val(supervise_id);
            var supervise_name = $(this).data('supervise_name');
            $(".supervise_name").html(supervise_name);
        });
    </script>
<?php $__env->stopSection(); ?>

<?php echo $__env->make('layouts.app', array_except(get_defined_vars(), array('__data', '__path')))->render(); ?>